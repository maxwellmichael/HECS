export type Feature = {
  id: number;
  icon: string;
  title: string;
  description: string;
};

export type Service = {
  id: number;
  title: string;
  description: string;
  image: string;
};

export type Product = {
  id: number;
  title: string;
  description: string;
  image: string;
  price: string,
  category: string,
};
